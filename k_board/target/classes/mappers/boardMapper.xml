<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kboard.mappers.boardMapper">

	<insert id="write" parameterType="com.kboard.domain.BoardVO">
	<!-- 파라미터 타입은 매퍼에 입력되는 데이터의 형태를 말하며, 리설트 타입은 매퍼가 내놓은 결과 데이터의
	 형태를 의미한다. -->
		insert into
		tbl_board(title, content, writer)
		values(#{title}, #{content}, #{writer})
	</insert>
	
	<update id="update" parameterType="com.kboard.domain.BoardVO">
		update
			tbl_board
				set
					title = #{title},
					content = #{content}
				where
						bno = #{bno}
	</update>
	
	<delete id = "delete" parameterType = "int">
		delete from
			tbl_board
				where
					bno = #{bno}
	</delete>
	
	<select id = "view" parameterType = "int" resultType="com.kboard.domain.BoardVO">
		select
			bno, title, content, writer, regDate, viewCnt
				from tbl_board
			where
					bno = #{bno}
	</select>
	
	<select id = "list" resultType="com.kboard.domain.BoardVO">
		select
			bno, title, content, writer, regDate, viewCnt
				from tbl_board
	</select>

</mapper>